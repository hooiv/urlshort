# Data Processing Agreement template

**Controller:** [Customer legal entity]
**Processor:** QuickLink / [service legal entity]

## Scope
The Processor provides link management, routing and analytics services on documented instructions from the Controller.

## Data categories
Only data required to operate the configured service is processed. Analytics can be configured for IP hashing, visitor hashing, user-agent storage, referrer storage and aggregate-only collection.

## Security
The service uses access control, audit logging, encrypted transport, hashed credentials, tenant-scoped queries, SSRF validation, rate limiting and incident response procedures.

## Sub-processors
[Insert current approved sub-processor list and regions before execution.]

## Retention and deletion
The Controller configures retention. Account deletion requests enter a seven-day grace period and are then processed by the privacy worker.

## Data subject rights
The service provides authenticated data export and deletion endpoints. The Controller remains responsible for responding to requests where legally required.

## International transfers
[Insert approved transfer mechanism and transfer impact assessment before execution.]

> This is an operational template, not legal advice. Have counsel approve it before signing.
