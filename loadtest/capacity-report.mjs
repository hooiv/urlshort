import fs from 'node:fs'
const files=process.argv.slice(2);if(!files.length)process.exit(0)
const summaries=files.map(f=>JSON.parse(fs.readFileSync(f,'utf8')));const rows=summaries.map((s,i)=>{const d=s.metrics||{};const h=d.http_req_duration?.values||{};const r=d.http_reqs?.values||{};const e=d.http_req_failed?.values||{};const rate=r.rate||0;const p95=h['p(95)']||0;const p99=h['p(99)']||0;const errorRate=e.rate||0;const budget=.001;return {file:files[i],requestsPerSecond:Number(rate.toFixed(2)),p95Ms:Number(p95.toFixed(2)),p99Ms:Number(p99.toFixed(2)),errorRate:Number(errorRate.toFixed(5)),errorBudgetRemaining:Number(Math.max(0,budget-errorRate).toFixed(5))}});console.log(JSON.stringify({generatedAt:new Date().toISOString(),assumptions:{targetErrorRate:0.001,capacityRule:'sustain the highest tested RPS while p95 <= 500ms and error rate <= 0.1%'},runs:rows},null,2));
