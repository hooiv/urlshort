export function versionAtLeast(actual:string|null|undefined, minimum:string|null|undefined):boolean{if(!minimum)return true;if(!actual)return false;const a=String(actual).trim().replace(/^v/i,'').split('.').map(x=>Number.parseInt(x,10)).map(x=>Number.isFinite(x)&&x>=0?x:0),m=String(minimum).trim().replace(/^v/i,'').split('.').map(x=>Number.parseInt(x,10)).map(x=>Number.isFinite(x)&&x>=0?x:0);for(let i=0;i<Math.max(a.length,m.length);i++){if((a[i]||0)>(m[i]||0))return true;if((a[i]||0)<(m[i]||0))return false}return true}
