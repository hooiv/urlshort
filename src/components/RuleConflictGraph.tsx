'use client'
import {useEffect,useState} from 'react'
type Node={id:string;label:string;priority:number;weight:number};type Edge={a:{id:string;name:string;priority:number};b:{id:string;name:string;priority:number};severity:'critical'|'warning';reason:string}
export default function RuleConflictGraph({shortCode}:{shortCode:string}){const[data,setData]=useState<{nodes:Node[];edges:Edge[]}|null>(null);useEffect(()=>{fetch(`/api/links/${encodeURIComponent(shortCode)}/routing/conflicts`).then(r=>r.ok?r.json():null).then(setData).catch(()=>null)},[shortCode]);if(!data)return null;const pos=data.nodes.map((n,i)=>({...n,x:30+(i%4)*220,y:30+Math.floor(i/4)*85}));const byId=new Map(pos.map(n=>[n.id,n]));return <section className="mt-8 rounded-2xl border border-slate-800 bg-slate-950/70 p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Routing conflict graph</h2><p className="mt-1 text-xs text-slate-500">Overlapping enabled rules are shown as edges.</p></div><span className="rounded-full border border-slate-800 px-2 py-1 text-xs text-slate-400">{data.edges.length} conflicts</span></div><div className="mt-5 overflow-auto"><svg viewBox={`0 0 ${Math.max(920,Math.ceil(pos.length/4)*220)} ${Math.max(150,Math.ceil(pos.length/4)*85)}`} className="min-w-[920px] h-[260px]">{data.edges.map((e,i)=>{const a=byId.get(e.a.id),b=byId.get(e.b.id);if(!a||!b)return null;return <line key={i} x1={a.x+70} y1={a.y+25} x2={b.x+70} y2={b.y+25} strokeWidth="2" className={e.severity==='critical'?'stroke-red-500':'stroke-amber-500'}/>})}{pos.map(n=><g key={n.id} transform={`translate(${n.x},${n.y})`}><rect width="140" height="50" rx="10" className="fill-slate-900 stroke-slate-700"/><text x="10" y="20" className="fill-slate-100 text-[11px]">{n.label.slice(0,20)}</text><text x="10" y="38" className="fill-slate-500 text-[9px]">P{n.priority} · {n.weight}%</text></g>)}</svg></div></section>}
