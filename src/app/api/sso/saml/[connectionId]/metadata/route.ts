import {NextRequest} from 'next/server';import {samlFor} from '@/lib/saml';export async function GET(request:NextRequest,{params}:{params:Promise<{connectionId:string}>}){const {connectionId}=await params;try{const s=await samlFor(connectionId);return new Response(s.generateServiceProviderMetadata(null),{headers:{'Content-Type':'application/samlmetadata+xml','Cache-Control':'public, max-age=3600'}})}catch{return new Response('SSO connection not found',{status:404})}}