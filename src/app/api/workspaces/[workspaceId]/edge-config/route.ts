import {NextRequest,NextResponse} from 'next/server';import {requireWorkspaceRole} from '@/lib/workspaces';import {publishWorkspaceRoutingConfig} from '@/lib/routing-config'
export async function POST(request:NextRequest,{params}:{params:Promise<{workspaceId:string}>}){const {workspaceId}=await params;const a=await requireWorkspaceRole(request,workspaceId,['owner','admin','editor']);if(!a.membership)return NextResponse.json({error:a.error},{status:a.status});const s=await publishWorkspaceRoutingConfig(workspaceId);return NextResponse.json({version:s.version,contentHash:s.contentHash,signature:s.signature,publishedAt:s.publishedAt})}