import { NextRequest, NextResponse } from 'next/server'
import { authenticateScim } from '@/lib/scim'
export async function GET(request:NextRequest,{params}:{params:Promise<{workspaceId:string}>}){const {workspaceId}=await params;if(!await authenticateScim(request,workspaceId))return NextResponse.json({schemas:['urn:ietf:params:scim:schemas:core:2.0:ServiceProviderConfig'],authenticationSchemes:[],patch:{supported:true},bulk:{supported:false},filter:{supported:true,maxResults:100},changePassword:{supported:false},sort:{supported:true},etag:{supported:false},meta:{resourceType:'ServiceProviderConfig'}},{status:401,headers:{'Content-Type':'application/scim+json'}});return NextResponse.json({schemas:['urn:ietf:params:scim:schemas:core:2.0:ServiceProviderConfig'],patch:{supported:true},bulk:{supported:false},filter:{supported:true,maxResults:100},changePassword:{supported:false},sort:{supported:true},etag:{supported:false},authenticationSchemes:[{type:'oauthbearertoken',name:'SCIM Bearer Token',description:'Workspace-scoped bearer token'}],meta:{resourceType:'ServiceProviderConfig'}},{headers:{'Content-Type':'application/scim+json'}})}
